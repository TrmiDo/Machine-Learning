Setting up

install anaconda, tensorflow, and pycharm

in the cmd

conda creat -n envName python=3.9
conda activate envName
pip install tensorflow
pip install keras


in pycharm

file > setting > project intepreter > setting > add > conda environmnet
navigate to anaconda environment envName, then choose the intepreter pythonw.exe

add configuration
+ > python. In script path, choose the file name.